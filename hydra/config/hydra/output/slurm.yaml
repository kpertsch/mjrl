# @package _global_
hydra:
  run:
    dir: /private/home/${env:USER}/outputs/${hydra.job.name}/${now:%Y-%m-%d}_${now:%H-%M-%S}
    subdir: ${hydra.job.override_dirname}
  sweep:
    dir: /private/home/${env:USER}/outputs/${hydra.job.name}/${now:%Y-%m-%d}_${now:%H-%M-%S}
    subdir: ${hydra.job.override_dirname}